server:
  port: 8889
spring:
  application:
    name: ms-admin-registry-server--2
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
         max-attempts: 20
         max-interval: 15000
         initial-interval: 10000
